const multiply = (n1:number, n2:number):number =>{
    // let value:number = 0;
    // for(let i=1; i<=n1; i++){
    //     value+=n2;}
    // return value;

    // return n1 / (1 / n2)

    return ("i").repeat(n1).repeat(n2).length
}

console.log(multiply(6,5))